#include "game.h"

class Freecell : public Game {
  public:
    Freecell();
    void add_card_from(Pile pile1, Pile pile2);
    void add_cards_from(Pile found1, Pile found2, int k);
    int legal_length(Pile found1, Pile found2);
};